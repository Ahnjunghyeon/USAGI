# 우사기 앱 리팩토링 로그

이 문서는 우사기의 제품 원칙, 변경사항, QA 결과, 배포 준비사항을 하나로 누적 관리합니다.

## 제품 원칙
- 실제 대화 FACT를 분석의 최우선 근거로 사용합니다.
- MBTI는 재미와 맥락 보조에만 사용하며 감정·호감·행동 원인을 단정하지 않습니다.
- 서비스 설명은 정중하게, AI 친구 한마디는 선택한 캐릭터에 맞춰 친근하고 코믹하게 표현합니다.
- 원본 캡처는 우사기 DB/파일 저장소에 영구 저장하지 않습니다. 외부 AI API 처리에는 전송됩니다.
- 숫자 지표는 가능한 한 TypeScript Metrics Engine에서 계산합니다.

## v0.3.4 — Production Safety & Reliability QA

### P0 수정 완료
1. Demo/Mock 제거
   - `/report/demo` 삭제
   - `ReportDemo.tsx`, `UploadMock.tsx`, demo helper 삭제
2. API Rate Limit 추가
   - IP 기준 1분 3회 / 1시간 20회
   - 429 + Retry-After 반환
   - 현재 인메모리 방식이므로 Vercel 다중 인스턴스 전체를 공유하지 않음. 공개 규모가 커지기 전에 Redis/KV 기반 전역 제한으로 교체 필요
3. 서버 입력 검증 강화
   - 관계/기간/분석 목표/나이대/성별/MBTI 허용값 검증
   - 프리셋 AI 친구의 persona/name/age/gender/MBTI는 서버 preset을 신뢰원으로 재구성
   - 직접입력 persona는 클라이언트 값을 사용하지 않음
4. 서버 이미지 제한
   - 최대 10장
   - 이미지 1장 data URL 약 1.25M chars 제한
   - 전체 약 3.6M chars 제한
5. Structured Outputs 적용
   - Vision 대화 구조화 JSON Schema
   - 최종 narrative JSON Schema
   - 출력 토큰 상한 분리
6. Prompt Injection 방어 강화
   - 캡처/대화 sample을 명령이 아닌 untrusted data로 명시
   - parser system instruction과 이미지 데이터를 분리
7. `분석 신뢰도` 제거
   - 실제 품질 척도로 오해되지 않도록 `데이터량: 적음/보통/충분`으로 변경
8. 업로드 순서 수정 기능
   - 캡처별 ↑/↓ 정렬 기능 추가
9. 중복 메시지 2차 제거
   - Vision dedupe 이후 TypeScript에서 인접 fingerprint dedupe 수행
10. Production 오류 메시지 정리
   - API Key/.env 가이드는 development에서만 표시
   - production에서는 내부 OpenAI 오류 내용을 노출하지 않음
11. 비용/운영 로그
   - 서버 로그에 model/input/output/total token, 추출 메시지 수 기록
   - 원본 대화 내용은 로그에 기록하지 않음

### QA에서 남은 P1
- 관계 유형별 결과 카드 세분화
- 전역 Rate Limit 저장소(Redis/KV) 연결
- usage/latency/status를 운영 DB에 집계
- OCR/화자 구분 테스트셋 30~100건 구축
- 실제 공유 이미지 생성 최적화
- 답장 검사 기능
- 실패한 분석 캡처의 보관/삭제 UX 정책 확정

### 배포 판단
- 로컬 개발: 가능
- GitHub 업로드: 가능 (`.env.local`, `.next`, `node_modules` 제외)
- Vercel 테스트 배포: 가능
- 불특정 다수 대규모 공개: 전역 Rate Limit과 실제 캡처 정확도 QA 후 권장

### 빌드 QA 상태
작업 환경에서 `npm install`이 제한시간 내 완전히 끝나지 않아 `@types/node`, `@types/react`, `@types/react-dom` 설치가 완료되지 않았습니다. 따라서 이 환경에서는 typecheck/build 최종 통과를 확인하지 못했습니다. 로컬에서 아래 세 명령을 필수 실행합니다.

```powershell
npm install
npm run typecheck
npm run build
```

## v0.3.6 — Cost Guard & Cancel QA

### 처리속도/비용
- 최종 AI 친구 narrative 기본 모델을 `gpt-5.4-mini`에서 `gpt-5.4-nano`로 변경했습니다.
- narrative에 전달하는 대화 sample을 최대 48개로 줄였습니다(앞 16 + 최근 32).
- narrative 출력 상한을 420 tokens로 줄였습니다.
- Vision 출력 상한을 4500 tokens로 줄였습니다.
- 두 단계 모두 `reasoning.effort=none`을 명시했습니다.
- usage 로그에 각 단계 처리시간(ms)을 기록합니다.

### 분석 취소
- Processing 화면에 `분석 취소하기` 버튼을 추가했습니다.
- 브라우저 `AbortController`를 사용해 `/api/analyze` 요청을 취소합니다.
- 서버의 `Request.signal`을 OpenAI API fetch에 전달합니다.
- Vision 완료 직후/최종 AI 호출 직전에 취소 여부를 재확인하여 취소 시 두 번째 유료 호출이 이어지지 않도록 했습니다.
- 취소 시 sessionStorage의 원본 캡처 및 결과 데이터를 제거합니다.
- 단, 이미 OpenAI가 처리한 구간의 사용량까지 0으로 되돌릴 수는 없으므로 UI에 해당 내용을 명시합니다.

### 비용/악용 보안
- 기본 IP 제한: 1분 2회 / 1시간 5회 / 1일 10회.
- 동일 IP에서 동시에 하나의 분석만 진행하도록 active lock을 추가했습니다.
- 전체 서비스 기준 일일 분석 하드캡 기본 100회를 추가했습니다.
- Upstash 사용 시 위 제한과 하드캡을 Vercel 인스턴스 전체에서 공유합니다.
- `USAGI_REQUIRE_DISTRIBUTED_GUARD=true` 설정 시 Upstash가 없거나 장애이면 fail-closed로 분석을 차단합니다.
- OpenAI 키는 계속 서버 환경변수로만 사용하며 클라이언트에 노출하지 않습니다.

### 공개 배포 권고
- Vercel 테스트 단계에서는 낮은 전역 한도를 유지합니다.
- 릴스/불특정 다수 공개 전 Upstash를 연결하고 `USAGI_REQUIRE_DISTRIBUTED_GUARD=true`를 활성화합니다.
- 실제 50~100회 호출의 usage/latency 로그를 기반으로 일일 한도를 조정합니다.


## v0.3.7 — Conversation-Specific Friend Answer QA

### AI 친구 답변 다양성/정확도
- 서로 다른 대화에서 같은 상투문구가 반복되지 않도록 friendComment 규칙을 강화했습니다.
- `분위기는 나쁘지 않아`, `혼자 마라톤`, `공 던져봐`, `조금 더 지켜봐`를 기본 템플릿처럼 반복하지 않도록 금지했습니다.
- 서버 계산 FACT에서 대화 균형, 상대/사용자 질문 반복, 웃음 표현, 짧은 핑퐁 등의 `conversationSignals`를 추가해 narrative 모델에 전달합니다.
- 최근 대화 내용 fingerprint를 이용해 동일한 분석 포맷이 반복되지 않도록 내부 style cue를 선택합니다. 같은 대화는 비교적 일관되고, 다른 대화는 서로 다른 관점으로 한마디를 구성합니다.
- 질문 반복, 대화 유지, 개인 지점 언급 등 여러 참여 신호가 함께 있을 때는 `어느 정도 관심이나 대화 의지는 있어 보여`와 같은 보수적인 방향성 판단을 허용합니다. 단, 이를 연애 감정 확정으로 표현하지 않습니다.
- AI 친구 한마디는 summary/highlights 복사가 아니라 `현재 대화에서 가장 특징적인 장면 → 판단 → 현재 주제와 연결된 행동 제안` 순서로 생성합니다.
- 행동 제안은 셀카/음식/약속/취미 등 실제 캡처에 나온 소재가 있으면 해당 소재에 연결하며, 항상 같은 `밥 먹자` 류의 조언을 반복하지 않습니다.

### 성능
- v0.3.6의 4단계 최적화(`gpt-5.4-nano`, reasoning none, sample 48개, 출력 상한 축소)는 그대로 유지합니다.
- 추가 신호는 TypeScript에서 계산하므로 별도의 AI 호출을 추가하지 않습니다.

## v0.3.8 — 모델 호환성 수정 + 브랜드 로딩 UX
- `gpt-5-mini`에 지원되지 않는 `reasoning.effort: none`이 전달되어 400 `unsupported_value`가 발생하던 문제 수정.
- Vision 단계는 `minimal`, GPT-5.4 nano 분석 단계는 `none`을 사용하며, `lib/openai.ts`에서 모델별 reasoning effort를 안전하게 정규화.
- 최신 메인 우사기 로고를 앱 아이콘/Apple 아이콘/브랜드 로고에 적용.
- 분석 중 기존 1→4 단계 진행 UI는 유지하고, 중앙에 무심한 우사기 + 안경이 내려와 착용되고 렌즈가 반짝이는 분석 애니메이션 추가.
- 완료 단계에 가까워질수록 초록 포인트가 자연스럽게 드러나도록 단계 카드 진행 모션 추가.
- `prefers-reduced-motion` 사용자에게는 애니메이션을 비활성화하고 정적 상태로 표시.


## v0.3.9 — 분석 마스코트 & MBTI 말투 축 리팩토링
- 검정 앱 아이콘 이미지를 분석 로딩 마스코트에서 제거하고, CSS로 만든 투명 배경의 뚱한 무표정 우사기로 교체했습니다.
- 안경이 위에서 내려와 착용되는 동작, 렌즈 피씽 효과, 미세한 집중 움직임을 하나의 마스코트 애니메이션으로 통합했습니다.
- prefers-reduced-motion 환경에서는 애니메이션을 정지한 상태로 제공합니다.
- AI 친구 MBTI의 T/F와 N/S 축을 말투 지시로 분리했습니다. T=직설적·논리적, F=공감적·관계 중심, N=가능성·연결 상상, S=구체적·현실 행동 중심으로 friendComment의 표현 방식에 반영합니다.
- MBTI는 말투와 조언 방식에만 영향을 주며 FACT, 수치, 관계 판단 자체에는 영향을 주지 않습니다.

## v0.3.11 — AI 친구 프리셋 동기화 및 UX QA

- `AGE_RANGES`에 `30대 중반`을 추가해 `모쏠 삼촌` 프리셋의 표시값과 실제 설정값을 일치시켰습니다.
- 프리셋 선택 시 수정 불가능한 친구 이름/나이/성별/MBTI 입력 폼의 중복 노출을 제거하고 선택된 친구 요약 카드로 대체했습니다.
- `직접입력`을 선택했을 때만 상세 친구 설정 폼을 노출합니다.
- 선택된 프리셋 카드 우측에 체크 표시를 추가해 선택 상태를 더 명확하게 표현합니다.
- 프리셋의 말투/캐릭터 설명은 유지하면서 화면 길이와 정보 중복을 줄였습니다.

## v0.3.12 — AI 친구 카드 중복 제거 및 디자인 QA

- 프리셋 선택 후 동일한 친구 정보가 두 번째 카드로 반복되던 영역을 제거했습니다.
- 선택 상태는 프리셋 카드의 핑크 테두리와 체크 아이콘 하나로만 표현합니다.
- 프리셋 선택 후에는 `이 친구는 이렇게 말해요` 설명만 남겨 말투 차이를 확인할 수 있도록 했습니다.
- `직접입력` 카드에서 동일 문구가 두 줄 반복되던 문제를 제거했습니다.
- 프리셋 카드의 높이, 내부 여백, 텍스트 계층, 선택 상태를 모바일 기준으로 미세 조정했습니다.

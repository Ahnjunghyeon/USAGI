# USAGI 통합 QA
# 우사기 앱 리팩토링 로그

이 문서는 우사기의 제품 원칙, 변경사항, QA 결과, 배포 준비사항을 하나로 누적 관리합니다.

## 제품 원칙
- 실제 대화 FACT를 분석의 최우선 근거로 사용합니다.
- MBTI는 재미와 맥락 보조에만 사용하며 감정·호감·행동 원인을 단정하지 않습니다.
- 서비스 설명은 정중하게, AI 친구 한마디는 선택한 캐릭터에 맞춰 친근하고 코믹하게 표현합니다.
- 원본 캡처는 우사기 DB/파일 저장소에 영구 저장하지 않습니다. 외부 AI API 처리에는 전송됩니다.
- 숫자 지표는 가능한 한 TypeScript Metrics Engine에서 계산합니다.

## v0.3.4 — Production Safety & Reliability QA

### P0 수정 완료
1. Demo/Mock 제거
   - `/report/demo` 삭제
   - `ReportDemo.tsx`, `UploadMock.tsx`, demo helper 삭제
2. API Rate Limit 추가
   - IP 기준 1분 3회 / 1시간 20회
   - 429 + Retry-After 반환
   - 현재 인메모리 방식이므로 Vercel 다중 인스턴스 전체를 공유하지 않음. 공개 규모가 커지기 전에 Redis/KV 기반 전역 제한으로 교체 필요
3. 서버 입력 검증 강화
   - 관계/기간/분석 목표/나이대/성별/MBTI 허용값 검증
   - 프리셋 AI 친구의 persona/name/age/gender/MBTI는 서버 preset을 신뢰원으로 재구성
   - 직접입력 persona는 클라이언트 값을 사용하지 않음
4. 서버 이미지 제한
   - 최대 10장
   - 이미지 1장 data URL 약 1.25M chars 제한
   - 전체 약 3.6M chars 제한
5. Structured Outputs 적용
   - Vision 대화 구조화 JSON Schema
   - 최종 narrative JSON Schema
   - 출력 토큰 상한 분리
6. Prompt Injection 방어 강화
   - 캡처/대화 sample을 명령이 아닌 untrusted data로 명시
   - parser system instruction과 이미지 데이터를 분리
7. `분석 신뢰도` 제거
   - 실제 품질 척도로 오해되지 않도록 `데이터량: 적음/보통/충분`으로 변경
8. 업로드 순서 수정 기능
   - 캡처별 ↑/↓ 정렬 기능 추가
9. 중복 메시지 2차 제거
   - Vision dedupe 이후 TypeScript에서 인접 fingerprint dedupe 수행
10. Production 오류 메시지 정리
   - API Key/.env 가이드는 development에서만 표시
   - production에서는 내부 OpenAI 오류 내용을 노출하지 않음
11. 비용/운영 로그
   - 서버 로그에 model/input/output/total token, 추출 메시지 수 기록
   - 원본 대화 내용은 로그에 기록하지 않음

### QA에서 남은 P1
- 관계 유형별 결과 카드 세분화
- 전역 Rate Limit 저장소(Redis/KV) 연결
- usage/latency/status를 운영 DB에 집계
- OCR/화자 구분 테스트셋 30~100건 구축
- 실제 공유 이미지 생성 최적화
- 답장 검사 기능
- 실패한 분석 캡처의 보관/삭제 UX 정책 확정

### 배포 판단
- 로컬 개발: 가능
- GitHub 업로드: 가능 (`.env.local`, `.next`, `node_modules` 제외)
- Vercel 테스트 배포: 가능
- 불특정 다수 대규모 공개: 전역 Rate Limit과 실제 캡처 정확도 QA 후 권장

### 빌드 QA 상태
작업 환경에서 `npm install`이 제한시간 내 완전히 끝나지 않아 `@types/node`, `@types/react`, `@types/react-dom` 설치가 완료되지 않았습니다. 따라서 이 환경에서는 typecheck/build 최종 통과를 확인하지 못했습니다. 로컬에서 아래 세 명령을 필수 실행합니다.

```powershell
npm install
npm run typecheck
npm run build
```

## v0.3.6 — Cost Guard & Cancel QA

### 처리속도/비용
- 최종 AI 친구 narrative 기본 모델을 `gpt-5.4-mini`에서 `gpt-5.4-nano`로 변경했습니다.
- narrative에 전달하는 대화 sample을 최대 48개로 줄였습니다(앞 16 + 최근 32).
- narrative 출력 상한을 420 tokens로 줄였습니다.
- Vision 출력 상한을 4500 tokens로 줄였습니다.
- 두 단계 모두 `reasoning.effort=none`을 명시했습니다.
- usage 로그에 각 단계 처리시간(ms)을 기록합니다.

### 분석 취소
- Processing 화면에 `분석 취소하기` 버튼을 추가했습니다.
- 브라우저 `AbortController`를 사용해 `/api/analyze` 요청을 취소합니다.
- 서버의 `Request.signal`을 OpenAI API fetch에 전달합니다.
- Vision 완료 직후/최종 AI 호출 직전에 취소 여부를 재확인하여 취소 시 두 번째 유료 호출이 이어지지 않도록 했습니다.
- 취소 시 sessionStorage의 원본 캡처 및 결과 데이터를 제거합니다.
- 단, 이미 OpenAI가 처리한 구간의 사용량까지 0으로 되돌릴 수는 없으므로 UI에 해당 내용을 명시합니다.

### 비용/악용 보안
- 기본 IP 제한: 1분 2회 / 1시간 5회 / 1일 10회.
- 동일 IP에서 동시에 하나의 분석만 진행하도록 active lock을 추가했습니다.
- 전체 서비스 기준 일일 분석 하드캡 기본 100회를 추가했습니다.
- Upstash 사용 시 위 제한과 하드캡을 Vercel 인스턴스 전체에서 공유합니다.
- `USAGI_REQUIRE_DISTRIBUTED_GUARD=true` 설정 시 Upstash가 없거나 장애이면 fail-closed로 분석을 차단합니다.
- OpenAI 키는 계속 서버 환경변수로만 사용하며 클라이언트에 노출하지 않습니다.

### 공개 배포 권고
- Vercel 테스트 단계에서는 낮은 전역 한도를 유지합니다.
- 릴스/불특정 다수 공개 전 Upstash를 연결하고 `USAGI_REQUIRE_DISTRIBUTED_GUARD=true`를 활성화합니다.
- 실제 50~100회 호출의 usage/latency 로그를 기반으로 일일 한도를 조정합니다.


## v0.3.7 — Conversation-Specific Friend Answer QA

### AI 친구 답변 다양성/정확도
- 서로 다른 대화에서 같은 상투문구가 반복되지 않도록 friendComment 규칙을 강화했습니다.
- `분위기는 나쁘지 않아`, `혼자 마라톤`, `공 던져봐`, `조금 더 지켜봐`를 기본 템플릿처럼 반복하지 않도록 금지했습니다.
- 서버 계산 FACT에서 대화 균형, 상대/사용자 질문 반복, 웃음 표현, 짧은 핑퐁 등의 `conversationSignals`를 추가해 narrative 모델에 전달합니다.
- 최근 대화 내용 fingerprint를 이용해 동일한 분석 포맷이 반복되지 않도록 내부 style cue를 선택합니다. 같은 대화는 비교적 일관되고, 다른 대화는 서로 다른 관점으로 한마디를 구성합니다.
- 질문 반복, 대화 유지, 개인 지점 언급 등 여러 참여 신호가 함께 있을 때는 `어느 정도 관심이나 대화 의지는 있어 보여`와 같은 보수적인 방향성 판단을 허용합니다. 단, 이를 연애 감정 확정으로 표현하지 않습니다.
- AI 친구 한마디는 summary/highlights 복사가 아니라 `현재 대화에서 가장 특징적인 장면 → 판단 → 현재 주제와 연결된 행동 제안` 순서로 생성합니다.
- 행동 제안은 셀카/음식/약속/취미 등 실제 캡처에 나온 소재가 있으면 해당 소재에 연결하며, 항상 같은 `밥 먹자` 류의 조언을 반복하지 않습니다.

### 성능
- v0.3.6의 4단계 최적화(`gpt-5.4-nano`, reasoning none, sample 48개, 출력 상한 축소)는 그대로 유지합니다.
- 추가 신호는 TypeScript에서 계산하므로 별도의 AI 호출을 추가하지 않습니다.

## v0.3.8 — 모델 호환성 수정 + 브랜드 로딩 UX
- `gpt-5-mini`에 지원되지 않는 `reasoning.effort: none`이 전달되어 400 `unsupported_value`가 발생하던 문제 수정.
- Vision 단계는 `minimal`, GPT-5.4 nano 분석 단계는 `none`을 사용하며, `lib/openai.ts`에서 모델별 reasoning effort를 안전하게 정규화.
- 최신 메인 우사기 로고를 앱 아이콘/Apple 아이콘/브랜드 로고에 적용.
- 분석 중 기존 1→4 단계 진행 UI는 유지하고, 중앙에 무심한 우사기 + 안경이 내려와 착용되고 렌즈가 반짝이는 분석 애니메이션 추가.
- 완료 단계에 가까워질수록 초록 포인트가 자연스럽게 드러나도록 단계 카드 진행 모션 추가.
- `prefers-reduced-motion` 사용자에게는 애니메이션을 비활성화하고 정적 상태로 표시.


## v0.3.9 — 분석 마스코트 & MBTI 말투 축 리팩토링
- 검정 앱 아이콘 이미지를 분석 로딩 마스코트에서 제거하고, CSS로 만든 투명 배경의 뚱한 무표정 우사기로 교체했습니다.
- 안경이 위에서 내려와 착용되는 동작, 렌즈 피씽 효과, 미세한 집중 움직임을 하나의 마스코트 애니메이션으로 통합했습니다.
- prefers-reduced-motion 환경에서는 애니메이션을 정지한 상태로 제공합니다.
- AI 친구 MBTI의 T/F와 N/S 축을 말투 지시로 분리했습니다. T=직설적·논리적, F=공감적·관계 중심, N=가능성·연결 상상, S=구체적·현실 행동 중심으로 friendComment의 표현 방식에 반영합니다.
- MBTI는 말투와 조언 방식에만 영향을 주며 FACT, 수치, 관계 판단 자체에는 영향을 주지 않습니다.

## v0.3.11 — AI 친구 프리셋 동기화 및 UX QA

- `AGE_RANGES`에 `30대 중반`을 추가해 `모쏠 삼촌` 프리셋의 표시값과 실제 설정값을 일치시켰습니다.
- 프리셋 선택 시 수정 불가능한 친구 이름/나이/성별/MBTI 입력 폼의 중복 노출을 제거하고 선택된 친구 요약 카드로 대체했습니다.
- `직접입력`을 선택했을 때만 상세 친구 설정 폼을 노출합니다.
- 선택된 프리셋 카드 우측에 체크 표시를 추가해 선택 상태를 더 명확하게 표현합니다.
- 프리셋의 말투/캐릭터 설명은 유지하면서 화면 길이와 정보 중복을 줄였습니다.

## v0.3.12 — AI 친구 카드 중복 제거 및 디자인 QA

- 프리셋 선택 후 동일한 친구 정보가 두 번째 카드로 반복되던 영역을 제거했습니다.
- 선택 상태는 프리셋 카드의 핑크 테두리와 체크 아이콘 하나로만 표현합니다.
- 프리셋 선택 후에는 `이 친구는 이렇게 말해요` 설명만 남겨 말투 차이를 확인할 수 있도록 했습니다.
- `직접입력` 카드에서 동일 문구가 두 줄 반복되던 문제를 제거했습니다.
- 프리셋 카드의 높이, 내부 여백, 텍스트 계층, 선택 상태를 모바일 기준으로 미세 조정했습니다.

## v0.3.13 — 단체톡 분석 & 관계 예외 QA
- 1:1 / 단체톡 분석 모드를 분리했습니다.
- 단체톡에서 참가자를 구분하고 사용자와 각 참가자의 연속 상호작용, 질문, 웃음 표현을 계산합니다.
- `우사기가 발견한 사람`과 참가자별 대화 특징 결과를 추가했습니다.
- 1:1↔단체톡 잘못 업로드한 경우 모드 전환을 안내합니다.
- 남성-남성, 여성-여성 등 성별 조합을 관계/성적 지향 추론 근거로 사용하지 않도록 강화했습니다.
- 단체톡에서도 호감·질투·연애 감정을 확정하지 않고 관찰된 상호작용만 해석합니다.
- Demo/Mock 파일을 제거했습니다.

## v0.3.14 — 공통 UI/로직 구조화 + 전체 QA

### 구조 리팩토링
- `components/ui/Button.tsx`: Primary / Secondary / Ghost 버튼을 `AppButton`, `ButtonLink`로 통합했습니다.
- `components/ui/FormControls.tsx`: label + input/select 반복 구조를 `TextField`, `SelectField`, `FieldGroup`으로 통합했습니다.
- `components/forms/MbtiSelect.tsx`: MBTI select를 독립 컴포넌트로 분리했습니다.
- `components/forms/PersonProfileFields.tsx`: 나/상대/단체톡 내 프로필의 나이대·성별·MBTI 폼을 공통화했습니다.
- `components/layout/AnalysisStepLayout.tsx`: 분석 1/2, 2/2 페이지의 네비게이션/타이틀/설명 레이아웃을 공통화했습니다.
- `lib/client/storage.ts`: localStorage/sessionStorage key와 JSON read/write/update/clear를 한 곳에서 관리합니다.
- `lib/upload-config.ts`: 클라이언트 이미지 압축 한도와 서버 검증 한도를 하나의 설정으로 공유합니다.
- 실제 사용되지 않던 `lib/friend.ts` Demo compatibility helper를 제거했습니다.
- 프리셋 관련 CSS의 구버전 중복 정의를 제거하고 v0.3.14 canonical style 블록 하나로 합쳤습니다.

### Legacy 빌드 방어
- `scripts/check-legacy.mjs`를 추가했습니다.
- `npm run build` 전에 `prebuild`에서 `ReportDemo.tsx`, `UploadMock.tsx`, `/report/demo` 잔존 여부를 검사합니다.
- 다른 PC에서 새 버전을 기존 폴더에 덮어써 오래된 Demo 파일이 남아도, 모호한 TypeScript 에러 대신 삭제할 파일을 먼저 알려줍니다.

### 유지보수 원칙
```
components/
  ui/        공통 버튼·폼 같은 원자 UI
  forms/     도메인 폼 묶음
  layout/    페이지 공통 레이아웃
  *.tsx      화면/도메인 컴포넌트
lib/
  client/    브라우저 저장소 등 클라이언트 전용 로직
  context.ts / analysis.ts / prompts.ts ... 도메인 로직
  upload-config.ts 클라이언트/서버 공통 업로드 한도
scripts/
  빌드 전 정적 QA 스크립트
```

### 전체 QA 확인
- 1:1 / 단체톡 context에 `mode`가 항상 포함되도록 확인했습니다.
- 남성-남성 / 여성-여성 등 성별 조합은 관계·성적 지향 판정 근거로 사용하지 않는 기존 규칙을 유지합니다.
- 단체톡의 참가자별 MBTI 입력은 요구하지 않으며 실제 상호작용 중심 분석을 유지합니다.
- OpenAI key, rate limit, 동시 실행 lock, global budget, AbortController 취소 구조는 변경하지 않았습니다.
- 실제 API 분석 로직과 FACT 계산은 UI 리팩토링과 분리하여 결과 변화를 최소화했습니다.
- TS/TSX 30개 파일 transpile 구문 검사: 오류 0건.
- legacy Demo/Mock 파일 검사: 통과.
- 현재 실행 환경의 내부 npm registry에서 `undici-types@6.21.0` 404가 발생하여 `npm ci` 및 Next production build는 이 환경에서 완료하지 못했습니다.
- 로컬에서는 `npm ci && npm run qa` 최종 통과를 확인해야 합니다.


### v0.3.13 전체 QA 원문

## 이번 버전 핵심
- 1:1 대화 / 단체톡 분석 모드 분리
- 단체톡에서 참가자별 발화자를 분리하고, 사용자와 각 참가자 사이의 연속 상호작용을 계산
- 단체톡 결과에 `우사기가 발견한 사람`, 참가자별 대화 특징, AI 친구 한마디 추가
- 1:1 모드에 단체톡을 올리면 단체톡 모드 전환 안내
- 단체톡 모드에 1:1을 올리면 1:1 모드 전환 안내
- 남성-남성 / 여성-여성 / 남성-여성 등 성별 조합 자체로 관계 또는 성적 지향을 추정하지 않도록 프롬프트 규칙 강화
- Demo/Mock 라우트 및 컴포넌트 제거

## 예외 상황 QA

### 1. 남성 ↔ 남성 / 여성 ↔ 여성 1:1
성별은 관계 판정 근거로 사용하지 않습니다.
사용자가 `연인`으로 설정한 경우 관계 맥락을 그대로 존중합니다.
`친구`로 설정했으나 상호 질문, 대화 재개, 개인적인 언급 등 특별한 상호작용이 보이면
`둘 사이 상호작용이 다른 대화보다 가까워 보인다` 수준으로만 표현하고 연애 감정이나 성적 지향을 단정하지 않습니다.

### 2. 단체톡을 1:1 모드로 업로드
Vision이 3명 이상을 구분하거나 다른 화자 ID가 2개 이상 감지되면 HTTP 422를 반환하고 `단체톡 모드` 전환을 안내합니다.

### 3. 1:1 대화를 단체톡 모드로 업로드
사용자 외 참가자가 2명 미만이면 HTTP 422를 반환하고 `1:1 모드` 전환을 안내합니다.

### 4. 단체톡에서 사용자 말풍선이 없음
사용자와 타인의 상호작용을 계산할 수 없으므로 분석을 중단하고 내 말풍선이 포함된 캡처를 요청합니다.

### 5. 단체톡 참가자 이름이 일부 가려짐
임의로 성격이나 신원을 추정하지 않고 `참가자1`, `참가자2` 같은 중립 라벨을 사용하도록 Vision 지침을 추가했습니다.
단, 동일 인물 여부가 이미지에서 불분명한 경우 화자 분리가 완벽하지 않을 수 있습니다.

### 6. 눈에 띄는 사람이 없음
`standoutName`을 null로 허용합니다. 무조건 한 명을 호감 대상으로 뽑지 않습니다.

### 7. "다른 사람도 느꼈을까?"
대화에서 눈에 띄는 티키타카가 있더라도 `다른 사람도 모두 눈치챘다`고 단정하지 않습니다.
`같은 방 사람도 눈치챘을 수는 있겠다` 수준의 가능성 표현만 허용합니다.

### 8. 캡처 중복 / 순서
기존 업로드 순서 조정과 인접 중복 제거를 유지합니다.
단체톡에서 서로 다른 참가자가 같은 짧은 문장을 연속으로 보낸 경우에는 참가자별 원본 메시지를 기준으로 상호작용 계산합니다.

### 9. Prompt Injection
캡처 안의 텍스트는 모두 신뢰할 수 없는 분석 데이터로 취급하며 명령으로 실행하지 않습니다.

### 10. 취소 / 비용 방어
기존 AbortController, 요청 제한, 동시 분석 제한, 글로벌 일일 가드 구조는 유지합니다.
취소 시 아직 시작하지 않은 다음 AI 호출을 차단하지만, 이미 OpenAI가 처리한 구간의 토큰 비용까지 0원으로 되돌리는 것은 보장할 수 없습니다.

## 현재 남은 한계
- 단체톡의 화자 인식은 프로필명, 말풍선 위치, 화면 맥락을 Vision이 해석하므로 이미지 품질과 캡처 방식에 영향을 받습니다.
- 카카오톡의 답장 인용 UI가 있는 경우 `누구에게 답했는지`를 별도 구조화하지 않으므로 현재 버전은 시간상 인접한 대화 흐름을 주요 신호로 사용합니다.
- 이미지 밖에서 이루어진 대화나 오프라인 관계는 알 수 없습니다.
- `standout`은 호감 점수가 아니라 업로드된 단체톡 안에서 사용자와 상호작용이 상대적으로 눈에 띄는 사람입니다.

## QA 실행 결과
- TS/TSX 24개 파일 TypeScript transpile 구문 검사: 오류 0건
- Demo/Mock 잔존 파일 검사: 0건
- `npm ci`: 현재 실행 환경의 내부 npm registry에서 `undici-types@6.21.0` 패키지 404로 설치 실패
- 따라서 이 환경에서는 Next.js production build 성공을 확정할 수 없습니다.
- 로컬에서 `npm ci && npm run qa`를 반드시 수행해야 합니다.

## v0.3.15 — Input UX Refactor

- 분석 시작을 3단계 흐름으로 재구성했습니다: 대화 입력 → 관계/맥락 → 상세 설정 및 분석.
- 카카오톡 복사 텍스트 파서를 추가했습니다. `[이름] [오전/오후 시:분] 메시지` 형식을 우선 로컬 파싱하고, 화자 수와 메시지 수를 즉시 보여줍니다.
- 텍스트 붙여넣기와 캡처 업로드를 첫 단계에서 선택하도록 했습니다.
- 텍스트에서는 참가자 수를 기준으로 1:1/단체톡을 자동 감지하고, 단체톡에서는 본인 화자를 선택할 수 있도록 했습니다.
- 단계별 우사기 안내 문구와 진행 UI를 추가해 설문처럼 보이는 입력 피로를 줄였습니다.
- 입력 초안은 sessionStorage에 유지해 단계 이동 시 정보가 사라지지 않도록 했습니다.

## v0.3.15 Clean Baseline Refactor
- 실제 사용 중인 v0.3.15 흐름만 남기고 preflight / ConversationDataset / image-dataset-manager 실험 코드를 제거했습니다.
- `ConversationInput` 안내 캐릭터를 `public/usagi-focus.png`로 변경했습니다.
- `ProcessingMascot`은 새 `public/usagi-guide-bunny.png`를 사용하며 안경 착용 및 렌즈 피쓩 애니메이션을 유지합니다.
- 모바일 Processing 상단 여백과 로딩 진행 표현을 정리했습니다.
- legacy QA 스크립트가 후속 실험 파이프라인 잔존도 감지하도록 확장했습니다.

## v0.3.15.1 — UI / Final Analysis Latency Refactor

- `usagi-focus.png`의 둥근 네모/그림자 효과를 제거해 투명 캐릭터 자체가 자연스럽게 보이도록 수정했습니다.
- `usagi-guide-bunny.png` 베이스 위 안경의 위치·폭·렌즈 크기를 눈 좌표에 맞게 다시 설계하고 착용/피쓩 애니메이션을 유지했습니다.
- 마지막 AI 해석 입력량을 줄이기 위해 1:1 샘플을 최대 36개(앞 12 + 최근 24), 단톡 샘플을 최근 36개로 축소했습니다.
- 단톡 AI 프롬프트에는 상호작용 상위 참가자 최대 4명만 전달하고 participantNotes도 최대 4명으로 제한했습니다.
- friendComment 목표를 120~170자로 줄이고 최종 output token 상한을 1:1 340 / 단톡 480으로 낮췄습니다.
- AI 친구 system instruction을 12개 핵심 원칙으로 통합해 중복 입력 토큰을 줄였습니다.
- 마지막 로딩 단계가 너무 일찍 나타나 오래 정지한 것처럼 보이지 않도록 단계 전환 간격을 1.9초로 조정했습니다.

### v0.3.15.2 — Structured JSON & Transparent Mascot Fix
- 최종 AI 응답의 `max_output_tokens`를 여유 있게 복구하되, 실제 답변 길이는 프롬프트의 120~170자 규칙으로 유지해 JSON 잘림을 방지합니다.
- Responses API가 `incomplete` 상태를 반환하면 JSON.parse 전에 감지해 원인을 명확하게 처리합니다.
- 잘못된 structured output은 `OpenAIError`로 정규화해 raw SyntaxError 500 대신 사용자 친화적인 재시도 오류로 처리합니다.
- 투명 배경 `usagi-guide-bunny.png` 사용을 전제로 `mix-blend-mode`, 외곽 drop-shadow, 배경 효과를 제거했습니다.

# USAGI v0.3.15.4 QA

## 반영
- 메인 우측 상단 Language selector: 한국어 / English / 日本語 / 中文.
- 선택 언어는 localStorage(`usagi-locale`)에 저장되고 이후 분석 화면과 결과 화면에 유지됩니다.
- 분석 요청에 locale을 전달해 AI summary/highlights/friendComment도 선택 언어로 생성하도록 지시합니다.
- 홈, 3단계 입력, 캡처 업로드, 분석 로딩, 결과 주요 UI를 4개 언어로 연결했습니다.
- 내부 관계/나이/성별/질문 값은 기존 한국어 canonical value를 유지해 분석 로직과 validation 호환성을 보존하고, UI에서만 번역합니다.

## 디자인 / 아이콘 / 모션 QA
- `public/usagi-focus.png` 존재 및 RGBA 투명 채널 확인.
- `public/usagi-guide-bunny.png`는 현재 RGB 파일로 실제 alpha 채널이 없습니다. CSS는 투명 배경 전제를 따르지만, PNG 자체 배경이 포함되어 있다면 이미지 파일 교체가 필요합니다.
- `usagi-guide-bunny.svg` 구형 참조 0건.
- 안경 drop → 착용 → lens spark 애니메이션 유지.
- 단계 완료 시 spring pop + ripple + 8방향 droplet + check 모션 유지.
- `prefers-reduced-motion` 대응 유지.
- 영어/일본어/중국어에서 긴 문구가 카드 높이를 깨지 않도록 AI 친구 카드 최소 높이와 word-break를 보정했습니다.

## 로직 QA
- `scripts/check-legacy.mjs`: PASS.
- `@/` 로컬 import 누락: 0건.
- UI에서 사용 중인 번역 key 중 한국어 dictionary 누락: 0건.
- 언어 선택은 canonical 분석 데이터 자체를 변경하지 않음.
- locale은 `/api/analyze`의 AI 해석 단계에만 반영되어 FACT/metrics 계산에는 영향 없음.

## 개선 권고
1. 현재 Processing 1→4 단계는 실제 서버 progress가 아니라 1.9초 타이머 기반입니다. 기능적으로 문제는 없지만 실제 진행률처럼 오인될 수 있어 추후 `읽기/계산/AI 응답` 실제 상태 이벤트로 전환 권장.
2. 서버 오류 메시지는 일부 한국어 canonical message가 그대로 반환될 수 있습니다. 다음 단계에서 error code 기반 클라이언트 번역으로 분리 권장.
3. 한국어 외 언어에서는 MBTI 장문 성향 설명을 숨기고 공통 참고 문구를 사용합니다. 16개 MBTI 전체 설명까지 완전 현지화하려면 별도 locale profile dictionary 추가 권장.
4. 대형 PNG(약 0.7~1MB)가 여러 개 있어 첫 로딩 최적화를 위해 WebP/AVIF 또는 적정 해상도 PNG 최적화 권장.

## 검증 제한
현재 전달 ZIP에는 `node_modules`가 포함되어 있지 않아 Next/React 타입을 갖춘 실제 `npm run build` 완료 검증은 이 환경에서 수행하지 못했습니다. 전역 TypeScript 실행은 Next/React 타입 부재로 유효한 프로젝트 빌드 판정에 사용할 수 없습니다.

# USAGI v0.3.15.5 QA Report

## 반영된 핵심 업데이트

- LocaleProvider의 저장 언어 복원이 완료된 뒤에만 분석 요청을 시작합니다.
- Processing 화면은 한 번 진입할 때 요청 흐름을 한 번만 시작하며, 202 진행 중 응답은 기존 작업을 재사용합니다.
- 서버는 입력 검증 → 캐시 확인 → 동일 작업 확인 이후에 Rate Limit과 전역 예산을 소비합니다.
- 동일한 대화·설정·언어 조합은 fingerprint 기반으로 10분간 결과를 재사용합니다.
- 분석 취소 시 입력·관계 설정·캡처를 삭제하지 않고 이전 설정 화면으로 돌아갑니다.
- 최종 structured output이 잘리거나 JSON이 깨지면 최종 해석 단계만 1회 자동 재시도합니다.
- 서버 입력 오류와 제한 관련 메시지를 한국어·영어·일본어·중국어로 제공합니다.
- 관계/인물 설정은 sessionStorage에 저장하며, 기존 localStorage 데이터는 1회 이동 후 삭제합니다.
- 사람 정보는 기본적으로 요약 표시하고 필요할 때만 펼쳐 수정합니다.
- 캡처 최대 업로드 수를 5장으로 조정했습니다.
- 텍스트와 이미지의 로딩 단계 전환 시간을 분리했습니다.
- 안경 착용 모션은 1회만 실행하고, 마지막 완료 단계에만 강한 물방울 burst를 사용합니다.

## 정적 QA 결과

- Legacy/Mock 파일 잔존: 없음
- 로컬 `@/` import 누락: 0건
- 이미지 에셋 누락: 0건
- 한국어 i18n 키 누락(정적 문자열 기준): 0건
- 영어 i18n 키 누락(정적 문자열 기준): 0건
- TypeScript 구문 오류: 없음 (`tsc`에서 TS100x 계열 오류가 더 이상 발생하지 않음)

## 검증 제한

현재 실행 환경의 내부 npm 레지스트리에서 `undici-types@6.21.0`을 가져오지 못해 `npm ci`와 실제 Next.js 전체 빌드를 완료하지 못했습니다. 사용자 로컬 환경에서 `npm run build` 최종 확인이 필요합니다.

# USAGI v0.3.15.7 App in Toss Policy Readiness QA

## 적용 항목
- 최초 이용 시 생성형 AI 사용 사실과 결과의 한계를 명시하는 동의 게이트 추가
- 분석 결과 화면에 `생성형 AI 분석 결과` 라벨 표시
- 공유 텍스트에도 AI 결과 및 참고용 고지 포함
- 캡처 업로드 전 권한 보유 및 민감정보 마스킹 확인 필수화
- 내부 서비스 안내 페이지 `/policy` 추가
- 브라우저에 저장된 입력·결과를 지우는 기능 추가
- 상대의 감정·호감·질투·관계를 확정하는 문구를 금지하는 프롬프트 강화
- 한국어 결과에서 일부 확정적 관계 표현을 서버 후처리로 완화
- 의료·법률·금융·심리 진단으로 오인될 표현 금지
- 4개 언어에 정책 관련 주요 문구 추가
- 외부 앱 설치, 외부 결제, 외부 로그인, 외부 광고 네트워크 의존 없음

## 정책 적합성 판단
- 제한 서비스(가상자산, 금융, 투자 자문, 의료 예약, 사행성)에 해당하지 않음
- 생성형 AI 사전 고지 및 결과 표시 의무를 UI에서 충족하도록 구성
- 미니앱 핵심 기능은 앱 내부에서 완결됨
- 외부 앱 설치 유도 및 외부 결제 흐름 없음

## 남은 운영 확인 사항
- 실제 개인정보 처리방침과 사업자/운영자 정보는 콘솔 제출 정보 및 법률 검토에 맞춰 별도 확정 필요
- OpenAI 등 외부 처리업체 사용 사실과 보관 기간은 실제 계약/설정에 맞춰 개인정보 처리방침에 정확히 기재 필요
- App in Toss SDK 적용 이후 토스 내 뒤로가기, 안전영역, 공유 기능을 실기기에서 추가 검증 필요


# USAGI v0.4.0.1 i18n Syntax Fix QA

- KO/EN/JA 번역 객체의 마지막 기존 항목 뒤 누락된 쉼표를 복구했습니다.
- 중국어 정책 번역 키가 DICTS 객체 안으로 잘못 삽입된 문제를 수정하고 ZH 번역 객체로 이동했습니다.
- DICTS 매핑을 `{ ko: KO, en: EN, ja: JA, zh: ZH }` 형태로 정상 복구했습니다.
- `lib/i18n.ts` 단독 TypeScript 구문 검증을 통과하도록 수정했습니다.

# USAGI v0.4.0 QA

## 적용
- 독자적인 Usagi Design System 토큰 도입
- 4/8 기반 spacing, typography, radius, motion 기준 통합
- 분석 입력 상단 progress header 적용
- Language 선택을 native select에서 접근 가능한 Bottom Sheet로 변경
- AI 친구 선택을 Bottom Sheet로 변경해 상세 화면 길이 축소
- 분석 진행 UI를 실제 완료/진행/대기 상태가 명확한 compact step list로 재설계
- 카드 그림자 최소화, border 중심의 App in Toss 친화 정보 계층 적용
- 실험용 temp 에셋 제거
- 기존 생성형 AI 고지, 결과 라벨, 개인정보 확인, 정책 페이지 유지

## 상업적 이용 안전성
- TDS 원본 아이콘/일러스트/컴포넌트 코드를 복제하지 않음
- 일반적인 모바일 UX 원칙만 독자적인 UDS로 구현
- 기존 Usagi 자체 캐릭터와 에셋만 사용

## 정적 QA
- @/ import 파일 존재 확인
- legacy Demo/Mock 파일 없음
- temp 에셋 제거
- package version 0.4.0


## 기준 버전
- 버전: `0.4.1`
- 기준: 사용자에게 노출되는 디자인, 사용 흐름, 생성형 AI 고지, 분석 안정성, 코드 유지보수성

## 1. 이번 릴리즈 변경 사항

### 결과 화면 정보 계층
- 생성형 AI 결과 배지를 독립된 첫 요소로 배치해 컨텍스트 칩과 겹치지 않도록 수정했습니다.
- 분석 조건 → 데이터 메타 → 핵심 제목 → 요약 → 지표 → 관찰 → AI 친구 의견 순으로 재구성했습니다.
- 화면 본문과 카드의 좌우 시작선을 `24px` 기준으로 통일했습니다.
- 결과 요약의 최대 읽기 폭을 제한해 긴 문장의 가독성을 개선했습니다.

### 카드와 지표
- 공통 `Card`, `Badge`, `ResultHeader`, `BalanceCard` 컴포넌트를 추가했습니다.
- 대화 밸런스에 나/상대 퍼센트 라벨을 추가했습니다.
- 질문 빈도와 평균 길이는 모바일에서 빠르게 비교 가능한 2열 통계 카드로 정리했습니다.
- 카드의 경계, 반경, 내부 여백을 디자인 토큰 기준으로 통일했습니다.

### 액션과 모션
- 하단 공유/재분석 액션이 콘텐츠를 가리지 않도록 결과 프레임 하단 안전 여백을 확보했습니다.
- Safe Area를 반영한 고정 액션 영역을 적용했습니다.
- 결과 요소는 짧은 Fade + Translate 모션으로 등장하며 `prefers-reduced-motion`을 지원합니다.

## 2. 기능 QA
- 기존 분석 API와 결과 데이터 타입은 변경하지 않았습니다.
- 1:1 및 단체톡 결과 렌더링 경로를 모두 유지했습니다.
- 결과 공유, 재분석, MBTI 피드백 기능을 유지했습니다.
- 생성형 AI 사전 고지, AI 결과 라벨, 업로드 동의, 정책 페이지를 유지했습니다.

## 3. 사용자 보호 QA
- 상대의 감정·호감·질투·관계를 확정하지 않는 기존 프롬프트 및 서버 후처리를 유지합니다.
- 결과는 생성형 AI의 참고용 분석임을 화면과 공유 텍스트에 표시합니다.
- 사용자가 업로드 권한과 개인정보 마스킹을 확인한 뒤 캡처 분석을 시작하도록 유지합니다.

## 4. 디자인 QA 체크리스트
- [x] AI 결과 배지와 컨텍스트 칩 겹침 제거
- [x] 본문·카드 좌우 정렬 통일
- [x] 제목·요약 간격 및 줄간격 개선
- [x] 카드 반경·테두리·내부 여백 통일
- [x] 하단 액션의 콘텐츠 가림 방지
- [x] 360px 내외 소형 모바일 대응
- [x] 모션 감소 설정 대응
- [x] 단체톡 결과 레이아웃 대응

## 5. 정적 코드 QA
- 공통 UI 컴포넌트를 도입해 결과 화면의 중복 마크업을 축소했습니다.
- 기존 QA 문서는 이 파일 하나로 통합했습니다.
- 레거시 Demo/Mock 검사는 기존 `scripts/check-legacy.mjs`를 유지합니다.

## 6. 로컬 최종 검증 명령
```powershell
npm run qa
npm run dev
```

실기기에서는 360px, 390px, 430px 너비와 iOS Safe Area 환경에서 결과 화면과 고정 액션 영역을 확인해야 합니다.

## 7. 이전 QA 문서 통합 이력
- `QA_REPORT_v0.3.15.4.md`
- `QA_REPORT_v0.3.15.5.md`
- `QA_REPORT_v0.3.15.7_APPINTOSS.md`
- `QA_REPORT_v0.4.0.1.md`
- `QA_REPORT_v0.4.0.md`

## v0.4.2 — 사용자 노출 UI/UX 전면 QA

### 적용 원칙
- 외부 디자인 시스템의 그래픽, 아이콘, 컴포넌트 코드를 복사하지 않았습니다.
- 일반적으로 활용 가능한 정보 계층, 일관된 여백, 명확한 CTA, 접근성, 반응형 설계 원칙만 우사기 고유 디자인 시스템에 적용했습니다.
- 우사기 로고·캐릭터·색상·브랜드 문구는 기존 독자 자산을 유지했습니다.

### 디자인
- 세로형 기준 논리 폭을 360~440px로 두고 페이지 좌우 여백을 토큰화했습니다.
- 결과 화면의 모든 텍스트·배지·카드·버튼 시작선을 통일했습니다.
- AI 결과 배지를 독립 행으로 배치해 조건 칩과 겹치거나 가려지지 않게 했습니다.
- 결과 조건과 메타 정보는 하나의 context panel로 묶고, 핵심 제목과 요약은 별도 copy block으로 분리했습니다.
- 작은 화면에서는 지표 2열을 1열로 전환합니다.
- Safe Area를 상단·하단에 적용했습니다.

### 인터랙션
- 버튼은 hover, active, focus-visible 상태를 모두 제공합니다.
- 터치 기기에서는 hover 이동 효과를 제거했습니다.
- 입력 필드는 hover/focus 상태를 명확히 구분합니다.
- 완료 체크의 장식성 물방울 burst를 제거하고 짧은 ripple만 유지했습니다.
- `prefers-reduced-motion` 환경에서는 애니메이션과 전환을 사실상 비활성화합니다.

### UX / 다크패턴 방지
- 서비스 진입 즉시 화면을 막던 생성형 AI 모달을 제거했습니다.
- 최초 AI 안내를 홈 CTA 위의 비차단형 안내 카드로 변경했습니다.
- 사용자는 안내를 닫거나 서비스 안내를 읽을 수 있으며, 기본 기능 탐색을 방해하지 않습니다.
- 분석 중 이탈 버튼은 실제 동작을 드러내는 `설정으로 돌아가기`를 유지합니다.
- 오류와 빈 상태 문구를 긍정적이고 다음 행동을 알 수 있는 해요체로 다듬었습니다.

### 그래픽
- 외부 제공 그래픽은 포함하지 않았습니다.
- 한 화면에서 핵심 우사기 그래픽 하나를 중심으로 사용하고 보조 요소는 작은 아이콘으로 제한했습니다.
- 의미 없는 파티클과 과한 장식 효과를 제거했습니다.

### QA 체크
- 기존 분석 API, 저장소, 비용 가드, 언어 처리 로직은 변경하지 않았습니다.
- 신규 내부 import 경로 정적 검사 통과.
- 구형 Demo/Mock 파일 없음.
- 로컬 최종 확인 명령: `npm run qa`.

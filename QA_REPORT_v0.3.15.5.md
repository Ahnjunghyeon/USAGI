# USAGI v0.3.15.5 QA Report

## 반영된 핵심 업데이트

- LocaleProvider의 저장 언어 복원이 완료된 뒤에만 분석 요청을 시작합니다.
- Processing 화면은 한 번 진입할 때 요청 흐름을 한 번만 시작하며, 202 진행 중 응답은 기존 작업을 재사용합니다.
- 서버는 입력 검증 → 캐시 확인 → 동일 작업 확인 이후에 Rate Limit과 전역 예산을 소비합니다.
- 동일한 대화·설정·언어 조합은 fingerprint 기반으로 10분간 결과를 재사용합니다.
- 분석 취소 시 입력·관계 설정·캡처를 삭제하지 않고 이전 설정 화면으로 돌아갑니다.
- 최종 structured output이 잘리거나 JSON이 깨지면 최종 해석 단계만 1회 자동 재시도합니다.
- 서버 입력 오류와 제한 관련 메시지를 한국어·영어·일본어·중국어로 제공합니다.
- 관계/인물 설정은 sessionStorage에 저장하며, 기존 localStorage 데이터는 1회 이동 후 삭제합니다.
- 사람 정보는 기본적으로 요약 표시하고 필요할 때만 펼쳐 수정합니다.
- 캡처 최대 업로드 수를 5장으로 조정했습니다.
- 텍스트와 이미지의 로딩 단계 전환 시간을 분리했습니다.
- 안경 착용 모션은 1회만 실행하고, 마지막 완료 단계에만 강한 물방울 burst를 사용합니다.

## 정적 QA 결과

- Legacy/Mock 파일 잔존: 없음
- 로컬 `@/` import 누락: 0건
- 이미지 에셋 누락: 0건
- 한국어 i18n 키 누락(정적 문자열 기준): 0건
- 영어 i18n 키 누락(정적 문자열 기준): 0건
- TypeScript 구문 오류: 없음 (`tsc`에서 TS100x 계열 오류가 더 이상 발생하지 않음)

## 검증 제한

현재 실행 환경의 내부 npm 레지스트리에서 `undici-types@6.21.0`을 가져오지 못해 `npm ci`와 실제 Next.js 전체 빌드를 완료하지 못했습니다. 사용자 로컬 환경에서 `npm run build` 최종 확인이 필요합니다.
